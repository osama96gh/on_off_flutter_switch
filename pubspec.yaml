name: on_off_switch
description: A Flutter package to create a custom on/off switch widget.
version: 0.0.1
repository: 'https://github.com/osama96gh/on_off_flutter_switch'

environment:
  sdk: '>=3.0.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
